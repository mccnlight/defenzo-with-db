import { BASE_URL } from '../app/services/api';

// API Configuration
export const API_CONFIG = {
  BASE_URL,  // Now correctly exported from api.ts
};

// Other configuration constants can be added here 